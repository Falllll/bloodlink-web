#!/bin/sh
set -e

export MSYS_NO_PATHCONV=1
repo="$(pwd -W 2>/dev/null || pwd)"

exec docker run --rm -v "$repo:/repo" -w /repo \
    zricethezav/gitleaks:v8.30.1 \
    git --pre-commit --staged --redact --no-banner --config=/repo/.gitleaks.toml